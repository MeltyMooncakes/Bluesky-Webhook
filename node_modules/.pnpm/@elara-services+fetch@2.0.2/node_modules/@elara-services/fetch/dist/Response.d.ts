/// <reference types="node" />
export declare class Response {
    private raw;
    private resOptions;
    body: Buffer | string;
    headers: Record<string, unknown>;
    statusCode: number;
    constructor(res: {
        headers: Record<string, unknown>;
        statusCode: number;
    }, resOptions: object);
    _addChunk(chunk: Uint8Array): void;
    json<T extends object>(): T | null;
    text(): string;
}
