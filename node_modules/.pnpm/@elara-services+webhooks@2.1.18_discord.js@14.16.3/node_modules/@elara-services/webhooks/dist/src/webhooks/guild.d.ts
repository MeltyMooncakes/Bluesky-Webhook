import type { Channel, Guild } from "discord.js";
import { type sendOptions } from "..";
export declare class GuildWebhook {
    guild: Guild;
    private client;
    private rest;
    constructor(guild: Guild);
    fetchWebhookInfo(opt: {
        name?: string | null | undefined;
        icon?: string | null | undefined;
    } | null | undefined): {
        username: string;
        avatar_url: string | null;
    };
    fetch(channel: Channel): Promise<import("..").CachedWebhook | null>;
    send(channelId: string, opt: sendOptions, queue?: boolean, shouldTransformComponents?: boolean): Promise<void | import("discord.js").APIMessage | null>;
    get disabled(): boolean;
}
