import { type sendOptions } from "..";
export declare let defaultWebhookOptions: {
    username: string;
    avatar_url: string;
};
export declare class Webhook {
    private clientId;
    private rest;
    constructor(botToken: string);
    fetchWebhookInfo(opt: {
        name?: string | null | undefined;
        icon?: string | null | undefined;
    } | null | undefined): {
        username: string;
        avatar_url: string;
    };
    fetch(channelId: string): Promise<{
        id: any;
        token: any;
    } | null>;
    send(channelId: string, opt: sendOptions, queue?: boolean, shouldTransformComponents?: boolean): Promise<void | import("discord-api-types/v10").APIMessage | null>;
    private fetchChannel;
}
